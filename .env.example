# Copy this file to `.env` (in the project root) and fill in the values.
# `.env` is gitignored, so secrets won't leak.
#
# The app loads this automatically at startup via python-dotenv.

# Required to use the "Calibrate" button. Get one at https://console.anthropic.com/.
ANTHROPIC_API_KEY=sk-ant-...
